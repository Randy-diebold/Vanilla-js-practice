const handleClick = () => console.log('clicked');

document.addEventListener('DOMContentLoaded', () => {
  // const button = document.getElementById('testButton');
  // button.addEventListener('click', (e) => console.log('clicked from app.js'));
});
